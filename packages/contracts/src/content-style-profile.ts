import { ContentStyleIntensity, ContentStylePreset, ContentTone, HookStyle, NarrationStyle, ScriptLanguage } from './enums.js';
export interface ProjectContentStyleProfile { projectId: string; preset: ContentStylePreset; primaryLanguage: ScriptLanguage; secondaryLanguage: ScriptLanguage | null; tone: ContentTone; narrationStyle: NarrationStyle; hookStyle: HookStyle; desiWordingLevel: ContentStyleIntensity; sarcasmLevel: ContentStyleIntensity; humorLevel: ContentStyleIntensity; energyLevel: ContentStyleIntensity; sensationalismLevel: ContentStyleIntensity; audienceDescription: string; preferredVocabulary: string[]; avoidedVocabulary: string[]; customInstructions: string; sensitiveTopicSarcasmEnabled: boolean; createdAt: string; updatedAt: string; }
export type ProjectContentStyleProfileUpdateInput = Omit<ProjectContentStyleProfile, 'projectId' | 'createdAt' | 'updatedAt'>;
export function normalizeContentStyleProfile(profile: ProjectContentStyleProfile) { return { preset: profile.preset, primaryLanguage: profile.primaryLanguage, secondaryLanguage: profile.secondaryLanguage, tone: profile.tone, narrationStyle: profile.narrationStyle, hookStyle: profile.hookStyle, desiWordingLevel: profile.desiWordingLevel, sarcasmLevel: profile.sarcasmLevel, humorLevel: profile.humorLevel, energyLevel: profile.energyLevel, sensationalismLevel: profile.sensationalismLevel, audienceDescription: profile.audienceDescription.trim(), preferredVocabulary: [...new Set(profile.preferredVocabulary.map(v => v.trim()).filter(Boolean))].sort(), avoidedVocabulary: [...new Set(profile.avoidedVocabulary.map(v => v.trim()).filter(Boolean))].sort(), customInstructions: profile.customInstructions.trim(), sensitiveTopicSarcasmEnabled: profile.sensitiveTopicSarcasmEnabled }; }
