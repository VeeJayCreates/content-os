import { integer, sqliteTable, text, uniqueIndex } from 'drizzle-orm/sqlite-core';
export const productionQueueItems = sqliteTable('production_queue_items', { id: text('id').primaryKey(), projectId: text('project_id').notNull(), opportunityId: text('opportunity_id').notNull(), researchPackageId: text('research_package_id').notNull(), status: text('status').notNull(), priority: integer('priority').notNull(), selectionScore: integer('selection_score').notNull(), selectionReason: text('selection_reason').notNull(), queuedAt: text('queued_at').notNull(), startedAt: text('started_at'), completedAt: text('completed_at'), failedAt: text('failed_at'), createdAt: text('created_at').notNull(), updatedAt: text('updated_at').notNull() }, (table) => [uniqueIndex('production_queue_items_opportunity_unique').on(table.opportunityId)]);
export type ProductionQueueItem = typeof productionQueueItems.$inferSelect;
