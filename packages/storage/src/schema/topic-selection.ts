import { integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";

export const projectSelectionPolicies = sqliteTable("project_selection_policies", { projectId: text("project_id").primaryKey(), minimumOpportunityScore: integer("minimum_opportunity_score").notNull(), minimumResearchConfidence: integer("minimum_research_confidence").notNull(), minimumIndependentSources: integer("minimum_independent_sources").notNull(), maxSelectedPerRun: integer("max_selected_per_run").notNull(), requireResearchPackage: integer("require_research_package", { mode: "boolean" }).notNull(), allowSingleSourceBreakingStories: integer("allow_single_source_breaking_stories", { mode: "boolean" }).notNull(), createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull() });
export const topicSelections = sqliteTable("topic_selections", { id: text("id").primaryKey(), projectId: text("project_id").notNull(), opportunityId: text("opportunity_id").notNull(), researchPackageId: text("research_package_id"), decision: text("decision").notNull(), selectionScore: integer("selection_score").notNull(), reason: text("reason").notNull(), evaluatedAt: text("evaluated_at").notNull(), createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull() }, (table) => [uniqueIndex("topic_selections_opportunity_unique").on(table.opportunityId)]);
export type ProjectSelectionPolicy = typeof projectSelectionPolicies.$inferSelect;
export type TopicSelection = typeof topicSelections.$inferSelect;
