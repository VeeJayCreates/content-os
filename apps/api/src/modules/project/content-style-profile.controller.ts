import { Body, Controller, Get, Param, ParseUUIDPipe, Put } from '@nestjs/common'; import { ContentStyleProfileService } from './content-style-profile.service'; import { UpdateContentStyleProfileDto } from './dto/update-content-style-profile.dto';
@Controller('projects') export class ContentStyleProfileController { constructor(private readonly service: ContentStyleProfileService) {} @Get(':id/content-style') get(@Param('id', new ParseUUIDPipe()) id: string) { return this.service.get(id); } @Put(':id/content-style') update(@Param('id', new ParseUUIDPipe()) id: string, @Body() body: UpdateContentStyleProfileDto) { return this.service.update(id, body); } }
