import { Body, Controller, Get, Param, ParseUUIDPipe, Patch } from '@nestjs/common'; import { UpdateSelectionPolicyDto } from './dto/update-selection-policy.dto'; import { SelectionPolicyService } from './selection-policy.service'; @Controller('projects') export class SelectionPolicyController { constructor(private readonly service: SelectionPolicyService) {} @Get(':id/selection-policy') get(@Param('id', new ParseUUIDPipe()) id: string) { return this.service.get(id); } @Patch(':id/selection-policy') update(@Param('id', new ParseUUIDPipe()) id: string, @Body() dto: UpdateSelectionPolicyDto) { return this.service.update(id, dto); } }
