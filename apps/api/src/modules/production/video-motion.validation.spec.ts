import { validateMotionScenes } from './video-motion.validation';
const scene=(patch:any={})=>({cameraMotion:'static',transition:'cut',overlays:[],map:null,...patch});
describe('video motion validation',()=>{it.each(['bad','zoom',''])('rejects invalid camera %s',cameraMotion=>expect(()=>validateMotionScenes([scene({cameraMotion})],[1000])).toThrow());it.each(['bad','slide'])('rejects invalid transition %s',transition=>expect(()=>validateMotionScenes([scene({transition})],[1000])).toThrow());it.each([{type:'bad',text:'x',startMs:0,endMs:1},{type:'title',text:'',startMs:0,endMs:1},{type:'title',text:'x',startMs:-1,endMs:1},{type:'title',text:'x',startMs:1,endMs:1},{type:'title',text:'x',startMs:0,endMs:1001}])('rejects invalid overlays',overlays=>expect(()=>validateMotionScenes([scene({overlays:[overlays]})],[1000])).toThrow());it.each([{latitude:91,longitude:0},{latitude:0,longitude:181},{latitude:NaN,longitude:0}])('rejects invalid markers',marker=>expect(()=>validateMotionScenes([scene({map:{focus:'India',markers:[marker]}})],[1000])).toThrow());it('rejects invalid routes',()=>{for(const points of [[],[{latitude:0,longitude:0}],[{latitude:0,longitude:0},{latitude:Infinity,longitude:0}]])expect(()=>validateMotionScenes([scene({map:{focus:'India',routes:[{points}]}})],[1000])).toThrow();});it('accepts generic no-map and valid map',()=>{expect(()=>validateMotionScenes([scene(),scene({map:{focus:'India',markers:[{latitude:20,longitude:77}],routes:[{points:[{latitude:20,longitude:77},{latitude:21,longitude:78}]}]}})],[1000,1000])).not.toThrow();});});
