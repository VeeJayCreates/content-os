'use client';
import * as React from 'react';
import type { Project, ProductionQueueItem } from '@content-os/contracts';
import { getProjects } from '@/features/projects/api/client';
import { fillProductionQueue, getProductionQueue, ResearchApiError } from '@/features/research/api/client';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
export function ProductionQueueScreen(){const [projects,setProjects]=React.useState<Project[]>([]);const [projectId,setProjectId]=React.useState('');const [items,setItems]=React.useState<ProductionQueueItem[]>([]);const [count,setCount]=React.useState(10);const [loading,setLoading]=React.useState(true);const [pending,setPending]=React.useState(false);const [error,setError]=React.useState<string|null>(null);const load=React.useCallback(async(id:string)=>{if(!id)return;setLoading(true);try{setItems(await getProductionQueue(id));setError(null)}catch(e){setError(e instanceof ResearchApiError?e.message:'Unable to load production queue.')}finally{setLoading(false)}},[]);React.useEffect(()=>{void getProjects().then(p=>{setProjects(p);if(p[0]){setProjectId(p[0].id);void load(p[0].id)}}).catch(()=>setError('Unable to load projects.'));},[load]);async function fill(){if(!projectId||pending)return;setPending(true);try{await fillProductionQueue(projectId,count);await load(projectId)}catch(e){setError(e instanceof ResearchApiError?e.message:'Unable to fill production queue.')}finally{setPending(false)}}return <section className="mx-auto max-w-6xl"><h1 className="text-3xl font-semibold">Production queue</h1><p className="mt-2 text-sm text-muted-foreground">Verified trending topics ready for the next production stage.</p><div className="mt-5 flex flex-wrap gap-2"><select className="h-9 rounded-md border bg-background px-3" value={projectId} onChange={e=>{setProjectId(e.target.value);void load(e.target.value)}}>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input className="h-9 w-20 rounded-md border bg-background px-2" type="number" min={1} max={50} value={count} onChange={e=>setCount(Math.min(50,Math.max(1,e.currentTarget.valueAsNumber||1)))}/><Button disabled={pending||!projectId} onClick={()=>void fill()}>{pending?'Filling…':'Fill queue'}</Button></div>{loading?<p className="mt-6">Loading…</p>:error?<Card className="mt-6"><CardContent className="pt-5">{error} <Button variant="outline" onClick={()=>void load(projectId)}>Retry</Button></CardContent></Card>:!items.length?<p className="mt-6 text-muted-foreground">No eligible topics are queued.</p>:<div className="mt-6 grid gap-3">{items.map(item=><Card key={item.id}><CardHeader><CardTitle>#{item.priority} {item.title}</CardTitle></CardHeader><CardContent className="text-sm text-muted-foreground">{item.verificationStatus} · {item.status} · queued {new Date(item.queuedAt).toLocaleString()}<p className="mt-2">{item.selectionReason}</p></CardContent></Card>)}</div>}</section>}
