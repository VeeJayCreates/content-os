"use client";
import * as React from "react";
import type { EditorialAssessment } from "@content-os/contracts";
import { assessEditorialFit, getEditorialAssessment, ResearchApiError } from "@/features/research/api/client";
import { Button } from "@/components/ui/button"; import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"; import { Badge } from "@/components/ui/badge";
export function EditorialAssessmentPanel({ opportunityId }: { opportunityId: string }) { const [assessment,setAssessment]=React.useState<EditorialAssessment|null>(null);const [loading,setLoading]=React.useState(true);const [error,setError]=React.useState<string|null>(null);const [pending,setPending]=React.useState(false);const requestId=React.useRef(0);const load=React.useCallback(async()=>{const request=++requestId.current;try{const next=await getEditorialAssessment(opportunityId);if(request===requestId.current)setAssessment(next);}catch(reason){if(request===requestId.current&&!(reason instanceof ResearchApiError&&reason.status===404))setError(reason instanceof ResearchApiError?reason.message:"Unable to load editorial assessment.");}finally{if(request===requestId.current)setLoading(false);}},[opportunityId]);React.useEffect(()=>{const timer=window.setTimeout(()=>void load(),0);return()=>{requestId.current+=1;window.clearTimeout(timer);};},[load]);async function assess(){if(pending)return;setPending(true);setError(null);try{setAssessment(await assessEditorialFit(opportunityId));}catch(reason){setError(reason instanceof ResearchApiError?reason.message:"Unable to assess editorial fit.");}finally{setPending(false);}}return <Card className="mt-6"><CardHeader><CardTitle>Editorial Assessment</CardTitle><CardDescription>Project-specific suitability. This is separate from Opportunity Score V2 and Research Confidence.</CardDescription></CardHeader><CardContent>{loading?<p className="text-sm text-muted-foreground">Loading editorial assessment…</p>:<div className="space-y-3">{assessment?<><div className="flex flex-wrap gap-2"><Badge>{assessment.status}</Badge>{assessment.editorialScore!==null?<Badge variant="success">Editorial Score {assessment.editorialScore}</Badge>:null}<Badge>Recommendation {assessment.recommendation??"Unavailable"}</Badge></div>{assessment.status==='ready'?<div className="grid gap-2 text-sm sm:grid-cols-2"><p>Relevance: {assessment.relevance}</p><p>Newsworthiness: {assessment.newsworthiness}</p><p>Content potential: {assessment.contentPotential}</p><p>Longevity: {assessment.longevity}</p><p>Duplication risk: {assessment.duplicationRisk}</p><p className="sm:col-span-2 text-muted-foreground">{assessment.rationale}</p></div>:null}</>:<p className="text-sm text-muted-foreground">No editorial assessment has been run.</p>}{error?<p role="alert" className="text-sm text-destructive">{error}</p>:null}<Button type="button" disabled={pending} onClick={()=>void assess()}>{pending?"Assessing…":assessment?"Re-assess editorial fit":"Assess editorial fit"}</Button></div>}</CardContent></Card>; }
